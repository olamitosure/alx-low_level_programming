#include "main.h"
#include <stdio.h>
/**
* _isdigit - check for a digit
* @c: list integer
* Return 1 or 0 if otherwise
*/
int _isdigit(int c)
{

	if (c >= '0' && c <= '9')
			c++;
	{
		return (1);
	}


		return (0);
}
