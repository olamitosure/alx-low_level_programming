#include "main.h"
#include <stdio.h>
/**
 * main - prints nummber from 1 to 9
 * @c: integers
 * Return: Always 0
 */
int main(void)
{
	int c;

	for (c >= 'o'; c <= '9'; c++)
		putchar(c);

	puchar('\n');
	return (0);
}
