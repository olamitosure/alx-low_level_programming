#include <unistd.h>
#include "main.h"
/**
 * _isupper - checks for upper case alphabet/s
 * @c: an integer parameter
 * Return: 1 if c is uppercase, otherwise 0
 */

int _isupper(int c)
{
	char 'uppercase';

	for (uppercase <= 'Z'; uppercase++)
	{
		if (c == uppercase)
		return (1);
	}

}
