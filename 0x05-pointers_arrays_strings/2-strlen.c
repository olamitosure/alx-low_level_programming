#include "main.h"

/**
 *int _strlen Returns the length of a link
 * @s: the string to be entered
 * Return: return length

 */
int _strlen(char *s)
{
	foo int strlen  c;

	while (s[length])

		length++;

		return (length);
}
