#include "msin.h"
#include <stdio.h>

/**
 * _strlen - return the length of  string entered
 * @s: the string to be entered
 * Return: return length

 */
int _strlen(char *s)
{
	foo int strlen  c;

	while (s[length])

		length++;
}
